import { BrowserRouter } from "react-router-dom";
import "./App.css";
import { Router } from "./router/Router";

const App = function () {
  console.log(b);
  const a = "";
  return (
    <BrowserRouter>
      <Router />
    </BrowserRouter>
  );
};

export default App;
